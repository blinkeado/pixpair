// This is a shim to handle importing embla-carousel-react
// It ensures the package can be properly imported in the build environment

import emblaCarousel from 'embla-carousel-react';

export default emblaCarousel; 