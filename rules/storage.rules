rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Temporarily allow all authenticated users to read/write everywhere
    // We'll make this more restrictive once we confirm functionality
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
} 